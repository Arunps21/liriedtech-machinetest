<% if (typeof user !== 'undefined' && user) { %>
<nav class="bg-white border-b border-gray-200 shadow-sm sticky top-0 z-50">
    <div class="w-full mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex items-center">
                <a href="/" class="text-xl font-bold text-gray-900 hover:text-blue-600 transition-colors">
                    Product Management System
                </a>
            </div>
            <div class="flex items-center space-x-4">
                <span class="text-sm font-medium text-gray-600">Welcome, <%= user.username %></span>
                <a href="/logout" class="bg-gray-100 hover:bg-gray-200 text-gray-700 px-4 py-2 rounded-md text-sm font-medium transition-colors border border-gray-300">Logout</a>
            </div>
        </div>
    </div>
</nav>
<% } %>
